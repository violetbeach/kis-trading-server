rootProject.name = 'kis-trading-server'

file('boot').eachDir { dir ->
    include dir.name
    project(":${dir.name}").projectDir = dir
}

file('domain').eachDir { dir ->
    include dir.name
    project(":${dir.name}").projectDir = dir
}