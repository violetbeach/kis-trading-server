spring:
  profiles:
    include:
      - test
      - auth
      - chart

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always

data-processing:
  target-stock-codes:
    "000001"
